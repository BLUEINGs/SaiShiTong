<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blueing.sports_meet_system.mapper.LoginRegisterMapper">
    <insert id="addUserSmByRegister">
        insert into user_sm (uid, sm_id, power_degree, joined_schools)
            value(#{uid},#{smId},#{powerDegree},#{joinedSchoolsStr})
    </insert>
    <insert id="addUserByRegister" useGeneratedKeys="true" keyProperty="uid">
        insert into users (name, password, head, sm_id) value(#{name},#{password},#{head},#{smId})
    </insert>
</mapper>